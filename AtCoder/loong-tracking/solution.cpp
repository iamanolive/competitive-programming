#include <iostream>
using namespace std;

int main() {
    int n, q; cin >> n >> q;
    for(int i = 0; i < q; i++) {
        int a, char b; cin >> a >> b;
        if(b != 'R' && b != 'L' && b != 'U' && b != 'D')
        else/////
    }
}